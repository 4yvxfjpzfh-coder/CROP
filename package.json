{
  "name": "crop",
  "private": true,
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "postinstall": "prisma generate --schema packages/prisma/schema/schema.prisma"
  },
  "devDependencies": {
    "prisma": "^6.19.3",
    "turbo": "^2.0.0"
  },
  "packageManager": "pnpm@10.34.5",
  "engines": {
    "node": "24.x"
  }
}


